<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.lijian.o2o.dao.ProductCategoryDao">

	<select id="queryProductCategory" resultType="com.lijian.o2o.entity.ProductCategory" parameterType="Shop" >
		SELECT
		shop_category_id,
		shop_category_name,
		shop_category_desc,
		shop_category_img,
		priority,
		
		create_time,
		last_edit_time,
		parent_id
		FROM tb_shop_category
		<where>
		<if test="shopCategoryCondition!=null">
		and parent_id is not null
		</if>
		<if test="shopCategoryCondition.parent!=null">
		and parent_id = #{shopCategoryCondition.parent.shopCategoryId}
		
		</if></where>
		ORDER BY
		priority DESC
	</select>
	<select id="queryProductCategoryByShopId" parameterType="long" resultType="ProductCategory">
		select
		product_category_id,
		product_category_name,
		priority
		from tb_product_category
		where shop_id=#{shopId}
		order by
		priority desc
	</select>
	<delete id="delProductCategory" parameterType="long" >
		delete
		from
		tb_product_category
		where product_category_id=#{productCategoryId}
	</delete>
<insert id="batchInsertProductCategory" parameterType="list">
	insert into tb_product_category(product_category_name,priority,create_time,shop_id)
	values
	<foreach collection="list" item="productImg" separator=",">
		(
		#{productImg.productCategoryName},
		#{productImg.priority},
		#{productImg.createTime},
		#{productImg.shopId}
		)
	</foreach>
</insert>
	<delete id="deleteProductCategory" parameterType="long">
		delete
		from
		tb_product_category
		where product_category_id=#{productCategoryId}
		and
		shop_id=#{shopId};

	</delete>
</mapper>
