<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.lijian.o2o.dao.ProductDao">

<insert id="addProduct" parameterType="product" keyProperty="productId" keyColumn="product_id" useGeneratedKeys="true">
	insert into
	tb_product
	(product_name,
	product_desc,
	img_addr,
	normal_price,
	promotion_price,
	priority,
	create_time,
	last_edit_time,
	enable_status,
	product_category_id,
	shop_id)
	values
	(#{productName},
	#{productDesc},
	#{imgAddr},
	#{normalPrice},
	#{promotionPrice},
	#{priority},

	#{createTime},
	#{lastEditTime},
	#{enableStatus},
	#{productCategory.productCategoryId},
	#{shop.shopId})
</insert>
	<insert id="addBatchProductImg" parameterType="list">

	insert into tb_product_img(img_addr,img_desc,priority,create_time,product_id)
	values
	<foreach collection="list" item="productCategory" separator=",">
		(
		#{productCategory.imgAddr},
		#{productCategory.imgDesc},
		#{productCategory.priority},
		#{productCategory.createTime},
		#{productCategory.productId}
		)
	</foreach>
	</insert>
</mapper>
